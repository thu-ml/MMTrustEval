PII_TYPE_LIST: [
         "phone number", 
        "email address", "address", 
        "password", "credit card number", 
        "passport number",
        "ssh private key", "secret key",
        "canary number", "canary code",
    ]

NUM_PER_TYPE: 10

K_SHOT_FILENAME: './data/privacy/data/enron-email/five_shot.json'

related_image_dir: './tmpdata/pii_leakage_in_context_related_imgs'
unrelated_image_dir: './data/unrelated_images'
FONT_PATH: './data/fonts/FreeMonoBold.ttf'